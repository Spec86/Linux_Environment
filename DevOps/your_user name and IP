1. Где найти your_server_ip (IP-адрес сервера):

    Если вы сами развернули сервер:
        После установки операционной системы на сервере IP-адрес обычно отображается на экране загрузки или в информации о сети.
        Используйте команду ip a на самом сервере, чтобы увидеть его IP-адрес:

    $ ip a

    Вы увидите адрес в разделе, начинающемся с inet (например, 192.168.1.100 для локальной сети или внешний адрес, если сервер подключён к Интернету).

Если сервер в облаке (например, DigitalOcean, Hetzner, OVH):

    IP-адрес обычно отображается в панели управления провайдера, где вы создавали сервер.

Если сервер размещён в локальной сети:

    Узнайте IP с помощью роутера или других устройств в сети, используя команды вроде:

        $ arp -a

2. Где взять your_user (пользователя для подключения):

    Если вы установили Ubuntu Server самостоятельно:
        Это имя пользователя, которое вы создавали во время установки системы.
        Чаще всего это что-то вроде ubuntu, admin или пользовательское имя, заданное вами.

    Если сервер создан в облаке:
        Многие облачные провайдеры создают пользователя по умолчанию:
            DigitalOcean: пользователь root.
            Hetzner: пользователь root.
            Vultr: пользователь root.
            Ubuntu Cloud Images (например, на VirtualBox или KVM): пользователь ubuntu.

    Если вы не уверены, попробуйте подключиться с пользователем root или ubuntu.

    Если сервер уже настроен другим администратором:
        Свяжитесь с администратором, чтобы узнать имя пользователя.

Пример ситуации:

    Ваш сервер имеет IP 192.168.1.50.
    Пользователь по умолчанию — ubuntu.

Тогда команда подключения будет выглядеть так:

% ssh ubuntu@192.168.1.50

3. Что делать, если ничего не знаете?

Если вы не уверены в IP-адресе или имени пользователя:

    Получите физический доступ к серверу (если это возможно):
        Зайдите в консоль сервера.
        Узнайте IP с помощью команды:

$ ip a

Посмотрите список пользователей:

        $ cut -d: -f1 /etc/passwd

        Это покажет всех пользователей на сервере. Обычно системные пользователи имеют UID меньше 1000.

    Обратитесь к провайдеру или администратору:
        Если сервер предоставлен хостинг-провайдером, уточните в их документации, какой IP-адрес и пользователь по умолчанию.

    Используйте DHCP-сервер или роутер:
        Если сервер находится в локальной сети, откройте панель управления роутера. Там вы увидите список всех устройств с их IP.

Если вы уточните, как и где был развернут сервер, я могу предложить более конкретный способ узнать эти данные. 😊
